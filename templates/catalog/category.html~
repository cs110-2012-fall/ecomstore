{% extends "catalog.html" %}

{% block content %}
<h1>{{ c.name }}</h1>
{{ c.description }}kkkkk
<br /><br /> 
	{% for p in products %}
	< div class="product_thumbnail">
		<a href="{{ p.get_absolute_url }}">
		
		{{ p.name }}
		</a>
	</div>
	
	{% endfor %}
{% endblock %}
